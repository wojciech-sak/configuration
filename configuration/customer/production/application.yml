server:
  port: 6666
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_service?serverTimezone=Europe/Warsaw&allowPublicKeyRetrieval=true&useSSL=False
    username: kodilla_user_microservices
    password: kodilla_password_microservices
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  allow-get-accounts: true